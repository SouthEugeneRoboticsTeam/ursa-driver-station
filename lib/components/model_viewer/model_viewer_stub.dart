import 'package:flutter/material.dart';

class ModelViewer extends StatelessWidget {
  const ModelViewer({super.key, double yaw = 0, double pitch = 0});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
